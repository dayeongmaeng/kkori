{
  "buildCommand": "expo export -p web",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "src": "/photos/([^/]+)", "dest": "/api/share-photo?externalId=$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
